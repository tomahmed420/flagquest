import Link from "next/link"; import {getCountries} from "../../lib/countries"; import {Search} from "lucide-react";
export default async function Learn(){const countries=await getCountries();return <main style={{padding:"44px 0 80px"}}><div className="container"><div style={{maxWidth:700}}><div style={{color:"#2563eb",fontWeight:800}}>LEARN</div><h1 style={{fontSize:48,letterSpacing:"-2px",margin:"8px 0 12px"}}>Explore countries</h1><p style={{color:"#667085",fontSize:17}}>Browse the world by country. Select any country to learn its flag, capital and key facts.</p></div><div className="card" style={{margin:"28px 0",padding:14,display:"flex",alignItems:"center",gap:10}}><Search size={20} color="#667085"/><input placeholder={`Search ${countries.length} countries...`} style={{border:0,outline:0,width:"100%",fontSize:16,background:"transparent"}} /></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(190px,1fr))",gap:14}}>{countries.map(c=><Link href={`/learn/${c.cca2.toLowerCase()}`} key={c.cca2} className="card" style={{padding:14,textDecoration:"none",color:"inherit",transition:"transform .2s"}}><img src={c.flag} alt={`${c.name} flag`} className="flag" style={{width:"100%",height:105,display:"block"}}/><div style={{fontWeight:800,fontSize:17,marginTop:13}}>{c.name}</div><div style={{color:"#667085",fontSize:13,marginTop:4}}>{c.region}{c.capital?.[0]?` · ${c.capital[0]}`:""}</div></Link>)}</div></div></main>}